**Agent:** 1.2

## Files touched

- `migrations/0001_create_resource.sql` (new)
- `src/db/schema.ts` (new)

## Commands run

- `npm run migrate:local` — exit 0
- `npm run migrate:local` again — exit 0, idempotent

## Result

Success. The schema matches the design section and the rollback script was verified.
